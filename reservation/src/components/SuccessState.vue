<script setup lang="ts">
defineProps<{
  message?: string;
}>();
</script>

<template>
  <div class="success-wrapper">
    <div class="icon-box">
      <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
        <circle class="checkmark__circle" cx="26" cy="26" r="25" fill="none"/>
        <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
      </svg>
    </div>
    
    <h2 class="success-title">Готово!</h2>
    <p class="success-message">{{ message || 'Бронювання успішно створено.' }}</p>
    <p class="sub-text">Вікно закриється автоматично...</p>
  </div>
</template>

<style scoped>
.success-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  box-sizing: border-box;
  padding: clamp(1.5rem, 5vw, 3rem);
  width: 100%;
  margin: auto; 
  flex: 1; 
  animation: fadeIn 0.5s ease;
}

.success-title {
  color: #fff;
  margin: clamp(1rem, 3vw, 1.5rem) 0 clamp(0.5rem, 1.5vw, 0.75rem) 0;
  font-size: clamp(1.5rem, 4vw, 2.2rem);
  font-weight: 600;
}

.success-message {
  color: #4CAF50;
  font-size: clamp(1rem, 2.5vw, 1.25rem);
  margin-bottom: 0.5rem;
}

.sub-text {
  color: #666;
  font-size: 0.9rem;
}

.checkmark {
  width: clamp(4rem, 15vw, 6rem);
  height: clamp(4rem, 15vw, 6rem);
  border-radius: 50%;
  display: block;
  stroke-width: 2;
  stroke: #4CAF50;
  stroke-miterlimit: 10;
  box-shadow: inset 0 0 0 #4CAF50;
  animation: fill .4s ease-in-out .4s forwards, scale .3s ease-in-out .9s both;
}

.checkmark__circle {
  stroke-dasharray: 166;
  stroke-dashoffset: 166;
  stroke-width: 2;
  stroke-miterlimit: 10;
  stroke: #4CAF50;
  fill: none;
  animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
}

.checkmark__check {
  transform-origin: 50% 50%;
  stroke-dasharray: 48;
  stroke-dashoffset: 48;
  animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
}

@keyframes stroke {
  100% { stroke-dashoffset: 0; }
}

@keyframes scale {
  0%, 100% { transform: none; }
  50% { transform: scale3d(1.1, 1.1, 1); }
}

@keyframes fill {
  100% { box-shadow: inset 0 0 0 1.875rem transparent; }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(0.625rem); }
  to { opacity: 1; transform: translateY(0); }
}
</style>